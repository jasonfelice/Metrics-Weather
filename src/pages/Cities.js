import React from 'react';
import Header from '../components/Header';

const Cities = () => (
  <>
    <Header heading="Cities" />
    <div>This is Cities Page!</div>
  </>
);

export default Cities;
